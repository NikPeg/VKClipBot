services:
  vkclipbot:
    build: .
    container_name: vkclipbot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./clips:/app/clips
      - ./state.json:/app/state.json
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
